def uv := import("lib/ml_libuv.so")

print("Without scheduler:\n")

parallel(1 .. 10; I) do
	for J in 1 .. 20 do
		print('\t[{I}]: {J}\n')
	end
end

print("\n\n\nWith scheduler:\n")

uv::run(;) do

parallel(1 .. 10; I) do
	for J in 1 .. 20 do
		print('\t[{I}]: {J}\n')
	end
end

end
